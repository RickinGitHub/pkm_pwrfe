"""Similarity graph scripts.

Holds scripts that build or refresh similarity edges in the knowledge
graph (BM25 today; vector similarity / community detection later).

NOTE: scripts currently in ``scripts/`` root (build_similarity_edges.py)
are NOT yet moved here — this package is prepared for a future migration.
"""
